<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.belette.bquiz" >
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ActiviteMenu"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ActiviteOption"
            android:label="Options" >
        </activity>
        <activity
            android:name=".ActiviteJouer"
            android:label="@string/title_activity_activite_jouer" >
        </activity>
        <activity
            android:name=".ActiviteThemes"
            android:label="@string/title_activity_activite_themes" >
        </activity>
        <activity
            android:name=".ActiviteQuiz"
            android:label="@string/title_activity_activite_quiz" >
        </activity>
    </application>

</manifest>
